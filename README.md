# HEAVY WEATHER

Official website of HEAVY WEATHER, an independent game studio.

The site is intentionally buildless: plain HTML, CSS, and JavaScript served
directly by GitHub Pages. All asset paths are relative, so the site also works
inside the repository path.

## Local preview

Open `index.html` in a browser or serve this directory with any static file
server.
